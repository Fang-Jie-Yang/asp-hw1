#include <stdio.h>

int main(void) {
	// TODO: Ctrl+c signal handler

	// main loop
	while(1) {
		// get a "line" of input

		// split into cmd by "|"

		// for each cmd, split by " "

			// handle cmd+arg

	}

	return 0;
}
